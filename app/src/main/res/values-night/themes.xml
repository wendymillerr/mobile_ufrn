<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema claro -->
    <style name="Theme.AplicativoSorteioKotlin" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/blue_500</item>
        <item name="colorOnPrimary">@color/white</item>

        <item name="android:colorBackground">@color/gray_700</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <!-- Tema escuro -->
    <style name="Theme.AplicativoSorteioKotlin.Dark" parent="Theme.Material3.Dark.NoActionBar">
        <item name="colorPrimary">@color/gray_700</item>
        <item name="colorOnPrimary">@color/white</item>

        <item name="android:colorBackground">@color/gray_900</item>
        <item name="android:textColor">@color/white</item>
    </style>

</resources>
